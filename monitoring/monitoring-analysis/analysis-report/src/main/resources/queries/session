{
    "size":%d,
    "query":{
        "bool":{
            "must":[
                {"term":{"%sapplication.id.raw":"%s"}},
                {"term":{"%ssession.sessionId.raw":"%s"}}
            ]%s
        }
    }
}